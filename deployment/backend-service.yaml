# backend-service.yaml
apiVersion: v1
kind: Service
metadata:
  name: instaff-backend
  labels:
    app: instaff-backend
spec:
  selector:
    app: instaff-backend
  ports:
    - name: http
      port: 80
      targetPort: 8080
  type: ClusterIP
